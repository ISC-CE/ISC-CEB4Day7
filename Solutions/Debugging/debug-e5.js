/*
Concept Tutorial:
Property names in JavaScript are case-sensitive.
Using the wrong case when accessing a property will result in 'undefined'.
*/

// Corrected Code:
console.log(obj.name);  // Corrected the property name to match the case
